# CLAUDE.md — grokchess

**The agent guidance for this repo lives in [`AGENTS.md`](AGENTS.md). Read that
file now, and follow it.**

This repo is worked on by several different AI coding agents (Claude Code,
Codex, and whatever anyone else brings). Keeping one set of instructions per
vendor guarantees they drift apart, and then the guidance an agent follows
depends on which filename it happened to load. So `AGENTS.md` — the
vendor-neutral convention — holds the real content, and this file exists only
to redirect the tools that look for `CLAUDE.md` by name.

**Do not add guidance here.** Put it in [`AGENTS.md`](AGENTS.md) so every agent
sees it.
